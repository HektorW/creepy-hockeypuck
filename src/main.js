
import Debug from './scripts/Debug';

import App from './scripts/App';

window.app = new App().init();

// Debug('Move: [w, a, s, d]');
// Debug('Shoot: [left mouse]');
// Debug('Change: [1, 2, 3]');
